@props(['name' => '', 'no' => '', 'icon' => '', 'alt' => ''])

<span id="{{ $name }}_{{ $no }}" name="{{ $name }}_{{ $no }}" class="lkn-numbered-button text-primary">
    <i class="{{ $icon }}"></i>
</span>
